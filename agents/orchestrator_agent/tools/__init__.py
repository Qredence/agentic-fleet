"""
Orchestrator Agent Tools Package
================================

The orchestrator agent does not use tools directly.
Instead, it delegates tasks to specialized agents that have their own tools.

This package is maintained for consistency with the agent structure.
"""
