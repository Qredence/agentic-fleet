name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write # Required for auto-fix commits on main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # Skip LFS downloads to avoid missing object errors
  # in microsoft/agent-framework. See:
  # https://github.com/microsoft/agent-framework/issues/1832
  GIT_LFS_SKIP_SMUDGE: "1"

jobs:
  # ============================================================================
  # Code Quality Checks (lint + type-check run in parallel)
  # ============================================================================
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v4
        with:
          # Need full history for auto-commit on main
          fetch-depth: 0
          # Use PAT or default token for push
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install uv
        uses: astral-sh/setup-uv@681c641aba71e4a1c380be3ab5e12ad51f415867 # v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Create uv virtual environment
        run: uv venv

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run Ruff linter (check only on PRs)
        if: github.event_name == 'pull_request'
        run: uv run ruff check . --output-format=github

      - name: Run Ruff formatter check (PRs only)
        if: github.event_name == 'pull_request'
        run: uv run ruff format --check .

      - name: Apply Ruff fixes (main branch)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          uv run ruff check . --fix --unsafe-fixes || true
          uv run ruff format .

      - name: Auto-commit fixes
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: stefanzweifel/git-auto-commit-action@8621497c8c39c72f3e2a999a26b4ca1b5058a842 # v5
        with:
          commit_message: "style: auto-fix lint and formatting issues [skip ci]"
          commit_user_name: "github-actions[bot]"
          commit_user_email: "github-actions[bot]@users.noreply.github.com"

  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v4

      - name: Install uv
        uses: astral-sh/setup-uv@681c641aba71e4a1c380be3ab5e12ad51f415867 # v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Create uv virtual environment
        run: uv venv

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run ty type checker
        run: uv run ty check src

  # ============================================================================
  # Test Suite (matrix for Python versions, macOS/Linux only per pyproject.toml)
  # ============================================================================
  test:
    name: Test (Python ${{ matrix.python-version }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.12"]
        include:
          # Test 3.13 only on Ubuntu to reduce CI time
          - os: ubuntu-latest
            python-version: "3.13"
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v4

      - name: Install uv
        uses: astral-sh/setup-uv@681c641aba71e4a1c380be3ab5e12ad51f415867 # v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
          cache-suffix: "v2" # Bump to invalidate stale caches

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Create uv virtual environment
        run: uv venv

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run tests
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          AZURE_AI_PROJECT_ENDPOINT: ${{ secrets.AZURE_AI_PROJECT_ENDPOINT }}
        run: uv run pytest -v --tb=short -m "not e2e" tests/

      - name: Run tests with coverage (main branch only)
        if: github.ref == 'refs/heads/main'
        run: uv run pytest -v --tb=short -m "not e2e" tests/ --cov=src/agentic_fleet --cov-report=xml

      - name: Upload coverage report
        if: github.ref == 'refs/heads/main' && matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: coverage-report
          path: coverage.xml
          retention-days: 7

  # ============================================================================
  # Frontend Tests
  # ============================================================================
  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/frontend
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v4

      - name: Setup Node.js
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "src/frontend/package-lock.json"

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Run tests
        run: npm test

  # ============================================================================
  # Agentic Workflow Validation
  # ============================================================================
  agentic-workflows:
    name: Agentic Workflows
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v4

      - name: Install gh-aw extension
        run: |
          gh extension install github/gh-aw || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Validate lock files are up-to-date
        run: |
          # Check that lock files exist and are valid YAML
          for lock_file in .github/workflows/*.lock.yml; do
            if [ -f "$lock_file" ]; then
              echo "Validating $lock_file..."
              python -c "import yaml; yaml.safe_load(open('$lock_file'))"
            fi
          done
          echo "All lock files are valid."

  # ============================================================================
  # Build Verification
  # ============================================================================
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, type-check, test, frontend, agentic-workflows]
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v4

      - name: Install uv
        uses: astral-sh/setup-uv@681c641aba71e4a1c380be3ab5e12ad51f415867 # v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Create uv virtual environment
        run: uv venv

      - name: Build package
        run: uv build

      - name: Check package metadata
        run: |
          uv pip install twine
          uv run twine check dist/*

      - name: Upload build artifacts
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: dist-packages
          path: dist/
          retention-days: 7

  # ============================================================================
  # Security Scan (runs in parallel with other jobs)
  # ============================================================================
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v4

      - name: Install uv
        uses: astral-sh/setup-uv@681c641aba71e4a1c380be3ab5e12ad51f415867 # v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Create uv virtual environment
        run: uv venv

      - name: Run Bandit security linter
        run: |
          uv pip install bandit[toml]
          # Fail CI only on MEDIUM+ severity AND MEDIUM+ confidence findings.
          # Also emit a JSON report so failures can be inspected via artifacts.
          uv run bandit -r src/ -c pyproject.toml -x src/frontend/node_modules -f json -o bandit-report.json -ll -ii

      - name: Upload security report
        if: failure()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: security-report
          path: bandit-report.json
          retention-days: 30
