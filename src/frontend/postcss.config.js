export default {
  plugins: {
    // Tailwind CSS is handled by @tailwindcss/vite plugin in vite.config.ts
    // No need for @tailwindcss/postcss when using the Vite plugin
    autoprefixer: {},
  },
};
