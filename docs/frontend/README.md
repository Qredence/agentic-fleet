```markdown
# Frontend docs

This folder contains an index and pointers for the frontend optimization docs that are kept at the repository root.

See `index.md` for a consolidated entry point and reading order.

Maintainers: move, reorganize, or update these files only after running `make validate-agents`.
```
